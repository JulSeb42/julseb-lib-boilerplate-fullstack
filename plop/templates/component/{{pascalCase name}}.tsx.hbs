import { Styled{{>pascalName }} } from "./styles"
import type { I{{>pascalName }} } from "./types"

export const {{>pascalName }}: FC<I{{>pascalName }}> = ({ {{#if forward}}ref,{{/if}} }) => {
    return (
        <Styled{{>pascalName }}{{#if forward}} ref={ref}{{/if}}>
            
        </Styled{{>pascalName }}>
    )
}